const pi = 3.14;
let radius = 3;
let area = 0;
area = radius * radius * pi;
console.log(`Radius: ${radius} == Area: ${area}`);
radius = 4;
area = radius * radius * pi;
console.log('Radius:', radius, '== Area:', area);